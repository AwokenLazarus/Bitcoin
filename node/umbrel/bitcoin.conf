# Umbrel Knots app datadir (Layout A — this is the live node).
# Path: ~/umbrel/app-data/bitcoin-knots/data/bitcoin/bitcoin.conf
# RPC 9332 / P2P 9333 stay on the app. Do not paste umbrel-bitcoin.conf here
# (it has Tor/RPC secrets).

includeconf=umbrel-bitcoin.conf
includeconf=blake2b.conf
